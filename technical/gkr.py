def gkr(df, g_threshold, t_threshold):
    '''
        select game line for small turnover rate
        input:
            df: dataframe for stock info
            g_threshold: low threshold for breakthrough_chips_rate (percent)
            t_threshold: high threshold for costs_chips_rate (percent)
        output:
            list:[(code, date, breakthrough_chips_rate, costs_chips_rate)]
    '''
    return list()

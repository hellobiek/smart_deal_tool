def rat(stock_df, index_df):
    '''
        rise against the index trend, means index is down, bug the stock price is up
        input:
            stock_df: dataframe for stock info
            index_df: dataframe for index info
        output:
            list:[(code, date, strength against index)]
    '''
    return list()

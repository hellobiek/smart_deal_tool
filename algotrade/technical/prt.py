def prt(df, p_threshold, t_threshold, peried):
    '''
        select stock which profit chips is down, and turn over chips in peried is very small
        input:
            df: dataframe for stock info
            p_threshold: low threshold for profit_chips_rate (percent)
            t_threshold: high threshold for turnover_chips_rate (percent)
            peroed: days in statics
        output:
            list:[(code, date, profit_chips_rate, turnover_chips_rate)]
    '''
    return list()

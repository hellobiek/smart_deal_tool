def bfp(df, d_threshold, u_threshold):
    '''
        select base float profit between d_threshold < base_float_profit < u_threshold
        input:
            df: dataframe for stock info
            d_threshold: low threshold for base float profit(percent)
            u_threshold: high threshold for base float profit(percent)
        output:
            list:[(code, date, base_float_profit)]
    '''
    return list()
